replicaCount: 2

image: morozovav82/product:1.0

service:
  type: NodePort
  port: 9000

appBasePath: '/product'

postgresql:
  fullname: users-postgresql
  postgresqlDatabase: users
  postgresqlUsername: users
  postgresqlPassword: qwerty123
  service:
    port: "5432"

mq:
  url: mq-artemis
  port: 61616
  user: artemis
  password: password

ingress:
  enabled: true
  hosts:
    - host: arch.homework
      paths: ["/product"]
      
metrics:
  serviceMonitor:
    enabled: false